# The harness lives here.
#
# Branch 0 has no harness: part7_index.py wires the pieces (browser, tools,
# context, loop) together by hand. Branch 2 moves that wiring -- plus owning the
# browser lifecycle -- into run_harness() here.
