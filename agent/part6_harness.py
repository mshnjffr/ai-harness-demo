# The harness lives here.
#
# This branch has no harness: part7_index.py wires the pieces (browser, tools,
# context, loop) together by hand. The `harness-owns-environment` branch moves
# that wiring -- plus owning the browser lifecycle -- into run_harness() here.
